apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kivo-staging
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/igor-siergiej/argonaut'
    targetRevision: HEAD
    path: 'apps/kivo/overlays/staging'
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: kivo-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
