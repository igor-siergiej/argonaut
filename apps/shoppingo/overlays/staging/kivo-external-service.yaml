apiVersion: v1
kind: Service
metadata:
  name: kivo
  namespace: shoppingo-staging
spec:
  type: ExternalName
  externalName: kivo.kivo-staging.svc.cluster.local
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
